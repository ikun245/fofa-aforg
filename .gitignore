# compiled binary (tracked intentionally for direct use)
# /fofa-afrog

# credentials — never commit these
.fofa-afrog.json

# scan output files
*.json
!go.sum

# Go workspace
go.work
go.work.sum

# OS
.DS_Store
Thumbs.db
